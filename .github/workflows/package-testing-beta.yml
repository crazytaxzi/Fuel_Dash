name: Package testing beta

on:
  push:
    branches:
      - main
    paths:
      - .github/beta-release-request.txt
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fuel-dash-testing-beta
  cancel-in-progress: false

jobs:
  package-and-release:
    runs-on: windows-latest

    steps:
      - name: Check out testing branch
        uses: actions/checkout@v4
        with:
          ref: testing
          fetch-depth: 0

      - name: Read beta metadata
        id: metadata
        shell: pwsh
        run: |
          $version = (Get-Content -LiteralPath BETA_VERSION -Raw).Trim()
          if ($version -notmatch '^v[0-9]{4}\.[0-9]{2}\.[0-9]{2}-beta\.[0-9]+$') {
            throw "BETA_VERSION has an invalid value: $version"
          }
          $targetSha = (git rev-parse HEAD).Trim()
          "version=$version" >> $env:GITHUB_OUTPUT
          "target_sha=$targetSha" >> $env:GITHUB_OUTPUT

      - name: Validate JavaScript
        shell: pwsh
        run: |
          $scripts = @(
            'app.js',
            'smart_data_loader.js',
            'missing_bol.js',
            'missing_bol_driver_only.js',
            'pdf_converter.js'
          )
          foreach ($script in $scripts) {
            if (Test-Path -LiteralPath $script) {
              node --check $script
              if ($LASTEXITCODE -ne 0) { throw "JavaScript validation failed for $script" }
            }
          }

      - name: Build clean beta ZIP
        id: package
        shell: pwsh
        run: |
          $version = '${{ steps.metadata.outputs.version }}'
          $targetSha = '${{ steps.metadata.outputs.target_sha }}'
          $dist = Join-Path $PWD 'dist'
          $packageRoot = Join-Path $dist 'Fuel_Dash-Beta'
          Remove-Item -LiteralPath $dist -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Path $packageRoot | Out-Null

          $excluded = @('.git', '.github', 'data', 'dist', 'Dashboard_Server_Log.txt')
          Get-ChildItem -LiteralPath $PWD -Force | Where-Object {
            $excluded -notcontains $_.Name
          } | ForEach-Object {
            Copy-Item -LiteralPath $_.FullName -Destination $packageRoot -Recurse -Force
          }

          $dataFolder = Join-Path $packageRoot 'data'
          New-Item -ItemType Directory -Path $dataFolder | Out-Null
          @'
Place the current XLSX and PDF report files in this folder.
The beta identifies supported reports by their contents, not their filenames.
Do not place confidential files in a package you intend to redistribute.
'@ | Set-Content -LiteralPath (Join-Path $dataFolder 'PLACE_REPORTS_HERE.txt') -Encoding UTF8

          @"
Fuel Dash Beta
Version: $version
Commit: $targetSha
Branch: testing
Built: $([DateTime]::UtcNow.ToString('u')) UTC

This is a beta build. Validate it with normal operating files before promoting it to main.
"@ | Set-Content -LiteralPath (Join-Path $packageRoot 'BETA_BUILD.txt') -Encoding UTF8

          $zipName = "Fuel_Dash-$version.zip"
          $zipPath = Join-Path $dist $zipName
          Compress-Archive -Path $packageRoot -DestinationPath $zipPath -CompressionLevel Optimal

          $hash = (Get-FileHash -LiteralPath $zipPath -Algorithm SHA256).Hash.ToLowerInvariant()
          $checksumPath = "$zipPath.sha256"
          "$hash  $zipName" | Set-Content -LiteralPath $checksumPath -Encoding ASCII

          "zip=$zipPath" >> $env:GITHUB_OUTPUT
          "checksum=$checksumPath" >> $env:GITHUB_OUTPUT

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: Fuel-Dash-${{ steps.metadata.outputs.version }}
          path: |
            ${{ steps.package.outputs.zip }}
            ${{ steps.package.outputs.checksum }}
          if-no-files-found: error

      - name: Publish prerelease
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = '${{ steps.metadata.outputs.version }}'
          $targetSha = '${{ steps.metadata.outputs.target_sha }}'
          $repo = '${{ github.repository }}'

          gh release view $version --repo $repo *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release delete $version --repo $repo --yes --cleanup-tag
            if ($LASTEXITCODE -ne 0) { throw "Could not replace existing beta release $version" }
          }

          gh release create $version `
            '${{ steps.package.outputs.zip }}' `
            '${{ steps.package.outputs.checksum }}' `
            --repo $repo `
            --target $targetSha `
            --title "Fuel Dash $version" `
            --notes-file BETA_RELEASE_NOTES.md `
            --prerelease

          if ($LASTEXITCODE -ne 0) { throw "GitHub prerelease creation failed" }

      - name: Keep only main and Beta-11 branches
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $repo = '${{ github.repository }}'
          $targetSha = '${{ steps.metadata.outputs.target_sha }}'
          $keepBranch = 'Beta-11'

          gh api "repos/$repo/git/ref/heads/$keepBranch" *> $null
          if ($LASTEXITCODE -eq 0) {
            gh api -X PATCH "repos/$repo/git/refs/heads/$keepBranch" -f sha=$targetSha -F force=true | Out-Null
          } else {
            gh api -X POST "repos/$repo/git/refs" -f ref="refs/heads/$keepBranch" -f sha=$targetSha | Out-Null
          }
          if ($LASTEXITCODE -ne 0) { throw "Could not create or update branch $keepBranch" }

          $branches = @(gh api --paginate "repos/$repo/branches?per_page=100" --jq '.[].name')
          if ($LASTEXITCODE -ne 0) { throw 'Could not list repository branches' }

          foreach ($branch in $branches) {
            if ($branch -eq 'main' -or $branch -eq $keepBranch) {
              continue
            }

            Write-Host "Deleting branch: $branch"
            gh api -X DELETE "repos/$repo/git/refs/heads/$branch" *> $null
            if ($LASTEXITCODE -ne 0) {
              $encodedBranch = [Uri]::EscapeDataString($branch)
              gh api -X DELETE "repos/$repo/git/refs/heads/$encodedBranch" *> $null
            }
            if ($LASTEXITCODE -ne 0) { throw "Could not delete branch $branch" }
          }
